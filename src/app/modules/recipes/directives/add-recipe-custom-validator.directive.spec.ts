import { AddRecipeCustomValidatorDirective } from './add-recipe-custom-validator.directive';

describe('AddRecipeCustomValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AddRecipeCustomValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
