import { RegisterFormCustomValidatorDirective } from './register-form-custom-validator.directive';

describe('RegisterFormCustomValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RegisterFormCustomValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
